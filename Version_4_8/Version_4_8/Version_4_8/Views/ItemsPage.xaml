<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Version_4_8.Views.ItemsPage"
             Title="Xamarin Froms 4.8">

    <ContentPage.Resources>
        <Style TargetType="Button">
            <Setter Property="BorderWidth" Value="1"/>
            <Setter Property="BorderColor" Value="Yellow"/>
            <Setter Property="BackgroundColor" Value="Orange"/>
            <Setter Property="TextColor" Value="White"/>
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>
    </ContentPage.Resources>

    <StackLayout Margin="20" Spacing="20" VerticalOptions="CenterAndExpand">
        <Button Text="Gradients and Brushes" CornerRadius="20" FontSize="16" Command="{Binding GradientsAndBrushesCommand}"/>
        <Button Text="Flyout Backdrop Color" CornerRadius="20" FontSize="16" Command="{Binding FlyoutBackdropColorCommand}"/>
        <Button Text="Drag &amp; Drop" CornerRadius="20" FontSize="16" Command="{Binding DragAndDropCommand}"/>
    </StackLayout>
</ContentPage>